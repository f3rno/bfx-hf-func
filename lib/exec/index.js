'use strict'

const execStrategyStep = require('./exec_strategy_step')
const execStrategyStepWithBreak = require('./exec_strategy_step_with_break')

/**
 * Functions for strategy execution
 *
 * @module Execution
 */
module.exports = {
  execStrategyStep,
  execStrategyStepWithBreak
}

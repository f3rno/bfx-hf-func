'use strict'

require('./strategy_state')

/**
 * A functional strategy compatible with the backtesting and execution methods
 * of this library.
 *
 * @typedef Strategy
 * @property {StrategyState} hff - state used for both backtesting and live execution
 */
